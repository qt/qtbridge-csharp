version: 2
tags: [git]
accept_configuration:
  condition: property
  property: features
  not_contains_value: Disable

instructions:

  Build:
    - type: Group
      instructions:
        - !include "{{qt/qtbridge-csharp}}/common_env.yaml"
        - !include "{{qt/qtbridge-csharp}}/build_csharp.yaml"
      enable_if:
        condition: property
        property: features
        contains_value: RunAsPlatformDependency
  Test:
    - !include "{{qt/qtbase}}/run_license_check.yaml"
